import { PlaceholderCard } from "@/components/shared/placeholder-card"

export function DashboardPlaceholder() {
  return (
    <PlaceholderCard
      title="Dashboard foundation ready"
      description="Layout, stores, schemas, mocks, and services are scaffolded."
    />
  )
}